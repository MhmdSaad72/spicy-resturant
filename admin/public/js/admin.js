$(function () {
    /* ==========================================
       CUSTOM FILE UPLOAD
    ============================================ */
    $(":file").filestyle({
        buttonBefore: true,
        btnClass: "btn-secondary font-weight-normal",
        placeholder: "No file chosen"
    });
});
